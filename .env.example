# Supabase Configuration
# Copy this file to .env.local for development
# NEVER commit .env files with actual keys to version control

# Public keys (safe to expose in client-side code)
EXPO_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Server-side only keys (NEVER expose to client)
# Only use these in server-side scripts or CI/CD
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Optional: Environment-specific settings
EXPO_PUBLIC_ENV=development
EXPO_PUBLIC_SENTRY_DSN=your-sentry-dsn-here
EXPO_PUBLIC_ANALYTICS_ENABLED=false

# Push Notification Services (future use)
# EXPO_PUBLIC_FCM_SENDER_ID=your-fcm-sender-id
# EXPO_PUBLIC_ONESIGNAL_APP_ID=your-onesignal-app-id

# App Store Configuration (for EAS Build)
# APPLE_TEAM_ID=your-apple-team-id
# ANDROID_PACKAGE_NAME=com.yourcompany.rostrdating